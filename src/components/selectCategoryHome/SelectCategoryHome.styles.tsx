import { StyleSheet } from 'react-native';

import { Colors } from 'utils/constants/Colors';

export const stylesSelectCategoryHome = StyleSheet.create({
    buttonOpenModal: {
        height: 40,
        backgroundColor: Colors.transparentGold,
        borderRadius: 15,
        marginHorizontal: 80,
        position: 'absolute',
        bottom: 75,
        left: 0,
        right: 0,
    },
});
