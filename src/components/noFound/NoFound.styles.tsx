import { StyleSheet } from 'react-native';

export const stylesNoFound = StyleSheet.create({
    wrapperTextNoFound: {
        height: '100%',
        alignItems: 'center',
        justifyContent: 'center',
    },
    textNoFound: {
        fontSize: 24,
    },
});
